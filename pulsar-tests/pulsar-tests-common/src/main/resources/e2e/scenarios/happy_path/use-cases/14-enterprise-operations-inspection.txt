# Use Case 14: Enterprise Operations Inspection
# Level: Enterprise
# Type: Internal systems, SSO authentication, observability
# Description: Enterprise operations dashboard inspection and monitoring
# Note: This use case is a TEMPLATE. Replace "internal admin or ops dashboard" and SSO details
#       with your organization's actual URLs, IdP, and credentials. Test only in non-production
#       environments first.

1. go to the internal admin or ops dashboard (replace with your actual URL, e.g. https://admin.example.com)
2. authenticate with enterprise SSO (replace with your IdP flow: Okta / Azure AD / etc.)
3. navigate across subsystems: check API gateway, database health, and job queue status pages
4. collect system status and key metrics (CPU, memory, error rate, queue depth)
5. compare metrics against baseline thresholds and flag anomalies
6. retry any failed health checks once
7. log all actions with timestamps to `ops-inspection-log.md`
8. export audit records as `ops-audit-<YYYY-MM-DD>.csv`
9. summarize findings and anomalies as `ops-inspection-report.md`
