/**
 * Pulsar All-in-one module.
 *
 * This module aggregates the core Pulsar dependencies into a single
 * convenience artifact. It pulls together pulsar-skeleton, pulsar-protocol,
 * and pulsar-ql so that downstream consumers can depend on a single
 * coordinate instead of listing each module individually.
 */
package ai.platon.pulsar.all;
